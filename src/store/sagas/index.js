/* eslint-disable no-constant-condition */
// import { all } from "redux-saga/effects";
// import { watcherAuth } from 'pages/Auth/state/sagas';

export default function* rootSaga() {
  // const sagas = [watcherAuth()];
  // yield all(sagas);
}
